const MagnificientEqualButton = ({ click }) => {
  return (
    // <div>
      <button value="=" onClick={click}>
        =
      </button>
    // </div>
  );
};

export default MagnificientEqualButton;
